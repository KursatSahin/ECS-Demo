using UnityEngine;

public class TankFields : MonoBehaviour
{
    public Transform m_FireTransform;           // A child of the tank where the shells are spawned.
    public Transform m_LeftFireTransform;       // A child of the tank where the left side fire shells are spawned.
    public Transform m_RightFireTransform;      // A child of the tank where the right side fire shells are spawned.
}